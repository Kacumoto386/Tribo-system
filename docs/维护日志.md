# Tribo 维护日志

## 2026-06-22 — v1.1.2 昵称显示修复 + 待办逻辑优化

### 操作类型
Bug 修复 + 功能优化

### 开发内容
修复多处显示 OpenID 而非昵称的问题；优化待办状态流转逻辑；评论数实时显示

### 技术债务 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 部署 | 阿里云 ECS | ICP 备案未完成，`kacumoto.com.cn` 无法访问 | 备案通过后 nginx SSL 正常，HTTPS 上线 |
| 2 | 备案 | 小程序代码包 | 备案平台需要代码包 MD5/SHA1 | 计算并提供 `a1bb32a2...` |
| 3 | 显示 | `team_detail.wxml` | 成员列表显示 `user_id` 而非昵称 | 改为 `{{item.nickname \|\| item.user_id}}` |
| 4 | 显示 | `team_detail.wxml` | 评论显示 OpenID | 改为 `{{item.nickname \|\| item.user_id}}` |
| 5 | 显示 | `team_detail.wxml` | 公告显示 `created_by` | 新增 `creator_nickname` 字段 |
| 6 | 显示 | `team_detail.wxml` | 留言显示 `user_id` | 新增 `nickname` 字段 |
| 7 | Schema | `schemas/team.py` | `ActivityCommentOut`/`TeamAnnouncementOut`/`TeamMessageOut`/`ActivityParticipantOut` 缺少昵称字段 | 均新增 `nickname` |
| 8 | 后端 | `routers/team.py` | 列表/创建接口未 JOIN User 表获取昵称 | 多处 JOIN + 返回昵称 |
| 9 | 后端 | `routers/user.py` | 新用户注册无昵称时存为空白 | 自动生成 `用户{id[-4:]}` |
| 10 | 显示 | `activity_detail.wxml` | 参与者列表/物资分派/采购认领/任务/布设项均显示 OpenID | 前端构建 `nickMap` 映射替换 |
| 11 | 逻辑 | `index.js` | 待办弹窗 `showTodoDialog` map 缺少 `assign` 类型，点击物资分派显示"暂无待办" | 新增 `assign: 'todoAssigns'` |
| 12 | 后端 | `routers/team.py` | `list_my_todos` 中采购认领/物资分派用 `status < 2`，标记完成后仍显示 | 改为 `status == 0`，只显示待处理项 |
| 13 | 后端 | `routers/team.py` | 物资分派标记完成后物资状态仍为"待准备" | 分派完成时自动联动更新物资状态 → "已到位" |
| 14 | 显示 | `team_detail.js` | 首次进入团队评论数固定为 0 | 改为后端返回 `comment_count` |
| 15 | 后端 | `routers/team.py` | `_build_activity_out` 未统计评论数 | 新增 `ActivityComment` 计数查询 |

### 版本
- 更新：`v1.1.1` → `v1.1.2`
- 配置文件：`backend/app/core/__init__.py`

---

## 2026-06-03 — v1.0.5c 活动管理增强

### 操作类型
功能开发 + Bug 修复

### 开发内容
活动下架/编辑、对话框互斥、分享转发、报名人数修正、WXML 标签修复

### 技术债务 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 逻辑 | `teams.js` | 创建/加入弹窗可同时打开，互相覆盖 | 打开一个时自动关闭另一个 |
| 2 | 统计 | `routers/team.py` | `_build_activity_out` 统计包含已取消报名，人数虚高 | 新增 canceled 计数，participant_count 减去已取消 |
| 3 | 编译 | `team_detail.wxml` | 多余 `</view>` 标签导致编译错误 | 删除尾部多余关闭标签 |
| 4 | 功能 | `team_detail.js/wxml` | 活动缺少编辑功能 | 新增编辑弹窗，调用已有 updateActivity API |
| 5 | 功能 | `team_detail.js/wxml` | 活动缺少下架功能 | 新增 status=4 下架，列表默认排除已下架 |
| 6 | 功能 | `team_detail.js/wxml` | 缺少分享入口 | 新增 share-btn + onShareAppMessage |
| 7 | 功能 | `routers/team.py` | 分享链路不完整 | 新增 auto-join 端点（团队/活动两个入口） |
| 8 | 功能 | `teams.wxml/js` | 邀请码不可点击复制 | 新增 📋复制 + wx.setClipboardData |
| 9 | 样式 | `team_detail.wxss` | 头部与 Tab 栏之间 100px 空白间距 | `.page` padding-bottom 归零 |
| 10 | 样式 | `team_detail/activity_detail/teams.wxss` | 弹窗缺少 dialog-mask/dialog-box 样式，非浮层形式 | 新增固定定位遮罩，弹窗顶部对齐 + 8vh 边距 |
| 11 | 导航 | `profile.js` | `wx.navigateTo` 无法跳转 Tab 页面（teams/special_events 是 TabBar 页） | 改为 `wx.switchTab` + `wx.setStorageSync` 传参 |
| 12 | 运行时 | `api.js` | 请求无超时限制，网络慢时导致 `WAServiceMainContext.js timeout` | 全局 `timeout: 8000`，失败快速返回 |
| 13 | 性能 | `profile.js` | `onLoad` 直接发 API 请求，可能阻塞页面初始化 | `onLoad` 仅从缓存加载，API 请求移到 `onShow` |

### 版本
- 更新：`v1.0.5` → `v1.1.1`
- 配置文件：`backend/app/core/__init__.py`

---

## 2026-06-03 — v1.0.5 UI 修复 + 线上部署

### 操作类型
UI 修复 + 服务器部署

### 开发内容
弹窗宽度/输入框高度修复、Tab 切换自动关闭弹窗、阿里云部署上线

### 技术债务 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 渲染 | `teams.wxml/wxss` | 创建团队对话框窄，输入框文字被截断 | 加 dialog-wide 类 + 全局 input-field 增高 |
| 2 | 逻辑 | `team_detail.js` | Tab 切换时弹窗未关闭 | switchTab 自动关闭所有弹窗 |
| 3 | 样式 | `teams.wxss` | 缺少 dialog-header/title/close 样式 | 补全弹窗通用样式 |
| 4 | 部署 | 阿里云 ECS | 后端搬迁至新服务器 | 配置 nginx + SSL + systemd 服务 |

### 版本
- 更新：`v1.0.4` → `v1.0.5`
- 配置文件：`backend/app/core/__init__.py`

---

## 2026-05-25 — v1.0.4 成员留言板

### 操作类型
功能开发（成员留言板）

### 开发内容
团队成员留言板功能：新表 team_messages + 3 个 API 端点 + 留言板对话框

### 技术债务 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 设计 | `team_detail.wxml` | 成员卡片缺少交互入口 | 添加 bindtap + 留言板 dialog |
| 2 | 权限 | `routers/team.py` | 留言端点校验双方都是团队成员 | 双重 TeamMember 查询 |

### 版本
- 更新：`v1.0.3` → `v1.0.4`
- 配置文件：`backend/app/core/__init__.py`

---

## 2026-05-25 — v1.0.3 活动深度扩展

### 操作类型
功能开发（活动详情 4 Tab + 物资/场地/任务模块）

### 开发内容
活动深度扩展 Phase 3：物资调配、场地布设、活动子任务、聚合详情端点、activity_detail 页面

### 技术债务 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 设计 | `activity_detail.wxml` | WXML 不支持 `assignments[item.id]` 括号属性访问 | JS 预计算嵌入到 resource 对象 `_assignments` |
| 2 | 编译 | `team_detail.wxml` | 活动卡片 bindtap 与内部按钮事件冲突 | 改用专用「详情」按钮替代整卡点击 |

### 版本
- 更新：`v1.0.2` → `v1.0.3`
- 配置文件：`backend/app/core/__init__.py`

---

## 2026-05-25 — v1.0.2 团队协作增强

### 操作类型
功能开发 + WXML 兼容性修复 + 数据库迁移

### 开发内容
团队协作 Phase 2：通知系统、团队动态 Feed、活动评论、团队公告

### 技术债务 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 编译错误 | 多处 `.wxml` | WXML 不支持 `[0].toUpperCase()` 方法调用 | 改为 JS 预计算 `initial` |
| 2 | 编译错误 | 多处 `.wxml` | WXML 不支持 `?.` 可选链运算符 | 去除可选链，改用预计算值 |
| 3 | 编译错误 | `team_detail.wxml` | WXML 不支持 `obj[key]` 括号属性访问 | 重构评论数据模型，嵌入活动对象 |
| 4 | 编译错误 | `teams.wxml` | WXML 不支持 `item.name[0]` 字符串索引 | 同上，预计算 `initial` |
| 5 | 样式问题 | `teams.wxml` | `animation-delay:{{index * 0.1}}s` 兼容风险 | 移除内联动画延迟 |
| 6 | 编译错误 | `app.json` | TabBar 引用 `.svg` 格式（仅支持 png/jpg） | 生成占位 PNG 图标 |
| 7 | 运行时 500 | `tribo.db` | 旧数据库缺少 Phase 2 新增字段 | 删库重建 |
| 8 | 网络错误 | `api.js` | `baseUrl` 端口 8011 与实际 8000 不匹配 | 修正为 8000 |
| 9 | 编译错误 | `special_events.wxml` | WXML 不支持 `eventTypes[key]` 动态对象访问 | 预计算 `typeTag` 字段 |

### 版本
- 更新：`v1.0.1` → `v1.0.2`
- 配置文件：`backend/app/core/__init__.py`

---

## 2026-05-24 — v1.0.1 系统功能审计与修复

### 操作类型
系统功能完整性审计 + Bug 修复

### 审计范围
- 后端 7 个路由模块、17 张 ORM 表、75+ API 端点
- 前端 15 个页面、5 Tab 导航、40+ API 封装
- 数据模型与 Schema 完整性
- 前后端接口对齐

### 发现 & 修复

| # | 类型 | 文件 | 问题 | 处理 |
|:-:|:----:|------|------|:----:|
| 1 | 数据缺失 | `api.js` + `habits.js` + `tasks.js` | 习惯打卡不传 `record_date` | 已修复 |
| 2 | 运行时错误 | `stats.py:197` | CSV 导出引用不存在的字段 | 已修复 |
| 3 | 渲染错误 | `special_events.wxml` | 变量名 `itemTypes` 未定义 | 已修复 |
| 4 | 逻辑错误 | `tasks.js` | 过滤条件使用不存在的 `is_completed` | 已修复 |
| 5 | 渲染错误 | `tasks.wxml` | `note` 字段后端不存在 | 已修复 |
| 6 | 渲染空白 | `ops_detail.wxml` | `desc`/`rules` 字段不存在 | 已修复 |
| 7 | 数据缺失 | `ops_detail.js` | `enrolled_count` 非模型字段 | 已修复 |
| 8 | 参数映射 | `tasks.js` | 创建任务 `note` → `description` | 已修复 |

### 遗留问题
- **ops.py** 使用 `X-User-ID` 头 + `db.commit()` 模式，与其他路由 JWT + `db.flush()` 不一致 —— 设计模式差异，不影响功能
- 部分页面缺少独立 `.wxss` 文件 —— 依赖全局样式，功能正常
- `README.txt` 文档中 API 数量与实际不符（49 → 75+）—— 需后续更新

### 版本
- 更新：`v1.0.0` → `v1.0.1`
- 配置文件：`backend/app/core/__init__.py`
