# Tribo 开发日志

## v1.1.5 (2026-06-04)

### 新功能

**活动结束时间：**
- TeamActivity 新增 `end_date` 字段（DateTime, nullable）
- 创建/编辑活动弹窗新增「结束时间」日期+时间并排选择器
- 活动详情页显示结束时间 `⏹ 2026-06-10 18:00`
- 自动状态流转使用 `end_date` 判断结束（优先），无 `end_date` 时回退 `activity_date + 24h`

**创建者自动签到：**
- 创建活动时自动报名+签到（`status=1, checkin_time=now`）
- 创建者无需手动操作，活动列表直接显示「已签到」

**体验优化：**
- 日期时间选择器改为并排布局（`dt-row` flex），减少视觉行数
- 首页启动时显示 `👋 昵称` 欢迎提示，确认登录身份
- 签到逻辑增加时间校验：活动未开始/已结束时拒绝签到

### Bug 修复
| 问题 | 处理 |
|------|------|
| `my_status=null` 导致报名按钮不显示 | JS 中 `null` → `undefined`，WXML `=== undefined` 匹配 |
| 微信 API 凭据被 `git stash` 清除 | 重新写入 `.env`，加 `.gitignore` 保护 |
| 旧数据库缺少 `end_date` 列 | `ALTER TABLE` 在线添加 |
| 创建场地 403 无权限 | `_check_activity_admin` 放宽为任何团队成员 |

### 后端改动
| 文件 | 变更 |
|------|------|
| `models/team.py` | TeamActivity 新增 `end_date` |
| `schemas/team.py` | Create/Update/Out 新增 `end_date` |
| `routers/team.py` | 自动状态流转 + 创建者自动签到 + 签到时间校验 + 权限放宽 |
| `core/__init__.py` | v1.1.4 → v1.1.5 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/team_detail/` | 结束日期/时间选择器 + dt-row 并排 + my_status null 修复 |
| `pages/activity_detail/` | 结束时间显示 |
| `pages/index/index.js` | 启动时欢迎提示 |
| `pages/profile/` | 版本号更新 |

---

## v1.1.4 (2026-06-03)

### 新功能

**退出团队：**
- 新增 `POST /api/teams/{team_id}/leave` 后端端点
- 校验：仅非群主成员可退出，群主需先撤回团队
- 退出后 `member_count` 递减，生成 Feed 记录
- 团队详情头部「退出」按钮，退出后跳转回团队列表

**实际参与者：**
- `GET /activities/{id}/full-detail` 新增 `participants` 字段，返回实际报名者（排除已取消）
- 活动详情「概览」Tab 参与人列表改为显示实际报名者
- 成员选择器仍使用全体成员（`getTeamMembers`），不影响分配功能

### 优化
- 首页头像移至左上角（`flex-start` 布局）
- 首页「我的团队」增加「＋创建」按钮，跳转团队页自动弹出创建弹窗
- 修复 `onTaskFilterChange` 重复定义导致任务筛选失效
- 修复删除物资时 `select` 误写为查询（改为 `delete()`）
- 修复 `currentUserId` 硬编码（改为从 Storage 读取）
- 修复撤回团队后 `navigateBack` 导航不可靠（改为 `switchTab`）
- 修复创建任务弹窗标签重复「优先级优先级」
- 删除死代码 `onAssignUserChange`、`filterProcurements`
- 重命名 `archive_announcement` → `delete_announcement` 与 HTTP 方法一致

### 后端改动
| 文件 | 变更 |
|------|------|
| `routers/team.py` | 新增 leave 端点 + full-detail 参与者 + delete 修复 |
| `schemas/team.py` | TeamActivityFullOut 新增 participants |
| `core/__init__.py` | v1.1.3 → v1.1.4 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/team_detail/` | 退出按钮 + 导航修复 + currentUserId 动态 |
| `pages/activity_detail/` | 参与者改用实际报名者 + 死代码删除 + 标签修复 |
| `pages/index/` | 头像左移 + 创建按钮 |
| `pages/teams/` | 接收 show_create 标志自动弹窗 |
| `utils/api.js` | leaveTeam 封装 |

---

## v1.1.3 (2026-06-03)

### 新功能

**成员选择器：**
- 任务/物资分派/布设项三处用户ID输入改为 picker 选择 + 手动输入双模式
- picker 列出团队成员（含群主/管理员标识），选择后自动填入
- 当前用户始终可见（标记为「我」），即使 API 未返回

**图标模块化：**
- 新增 `iconAvatar` / `iconBox` 两个 WXML 模板，背景+图案封装一体
- 首页团队头像、快速入口、动态图标全部改用模板
- 修复动态铃铛图标垂直偏移（`<text>` 不支持 flex → 改为 `<view>` 包裹）

**体验优化：**
- 加入团队成功后自动跳转到团队详情页
- 用户 ID 简化为纯数字（`dev_user_001` → `10001`），保留向后兼容
- 用户 ID 可点击复制（个人资料页）
- 日期格式优化：去掉 `T`，截断到分钟（`2026-06-03 17:00`）

### 后端改动
| 文件 | 变更 |
|------|------|
| `core/__init__.py` | v1.1.2 → v1.1.3 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/activity_detail/activity_detail.js/wxml` | 成员选择器 + 当前用户兜底 |
| `pages/teams/teams.js` | 加入团队后跳转详情 + 用户ID简化 |
| `pages/index/index.wxml/wxss` | 图标模板化 + feed-icon flex 修复 |
| `pages/profile/profile.js/wxml` | ID 复制 + 版本号 |
| `utils/api.js` | 全局 timeout |
| `app.js` | baseUrl 切换 |

---

## v1.1.2 (2026-06-03)

### 新功能

**活动状态自动流转：**
- 到活动时间 → 自动变为「进行中」
- 超过 24 小时 → 自动变为「已结束」
- 查询活动时触发检查（`_update_activity_status`），无需定时任务
- 手动编辑仍可随时覆盖状态

**体验优化：**
- 活动卡片整卡可点击 → 跳转到活动详情页（`catchtap` 防止按钮冒泡）
- 日期格式优化：`2026-06-03T17:00:00` → `2026-06-03 17:00`（JS 预计算 `_displayDate`）
- 群主标识修复：硬编码 `dev_user_001` → 动态获取当前用户 ID
- Tab 页面导航修复：`wx.navigateTo` → `wx.switchTab` + `wx.setStorageSync` 传参
- API 请求全局 `timeout: 8000`，防止请求挂死

### 后端改动
| 文件 | 变更 |
|------|------|
| `routers/team.py` | 新增 `_update_activity_status` 自动状态流转 + 修复时区比较 |
| `core/__init__.py` | v1.1.1 → v1.1.2 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/team_detail/` | 活动卡片绑定 `goToActivity` + `catchtap` 防冒泡 + `_displayDate` |
| `pages/activity_detail/` | 日期显示 `_displayDate` |
| `pages/teams/` | 群主标识动态 `currentUserId` |
| `pages/profile/` | 导航修复 `wx.switchTab` |
| `utils/api.js` | `getTeams(archived)` 参数 + 全局 timeout |

---

## v1.1.1 (2026-06-03)

### 新功能

**采购管理（第 5 Tab）：**
- 双表模式（采购项 + 认领记录），支持物品清单/认领/购买/确认全流程
- 状态自动流转：待采购 → 部分认领 → 已购齐 → 已完成
- 认领数量校验，防止超额
- 预估/实际花费对比，AA 分摊计算
- 小票 URL 记录

**团队管理增强：**
- 团队编辑（名称/描述），仅群主可操作
- 团队撤回/恢复，撤回后仅群主在「已撤回的团队」中可见
- 「我的」页面新增「📂 已撤回的团队」入口
- 团队列表页支持归档模式切换

**活动管理增强：**
- 活动编辑弹窗（标题/描述/地点/时间/人数）
- 活动下架/重新上架（status=4），列表默认隐藏
- 报名状态（my_status）后端返回，前端正确显示签到/报名按钮
- 报名人数统计排除已取消记录

**分享与邀请：**
- 团队详情右上角转发按钮（微信原生分享）
- 点击分享卡片自动加入团队（auto-join 端点）
- 邀请码一键复制（团队列表 + 团队详情）

**UI/UX 优化：**
- 全局弹窗 dialog-mask/dialog-box 样式补全，固定定位浮层 + 顶部对齐
- 创建/加入团队弹窗互斥
- 切换 Tab 自动关闭所有弹窗
- 团队详情头部间距优化
- 活动卡片按钮紧凑化
- 修复 Tab 页面导航问题（wx.navigateTo → wx.switchTab）

### Bug 修复
| 问题 | 处理 |
|------|------|
| 成就页 500（UserAchievement 重复记录） | scalar_one_or_none → scalars().first() |
| WAServiceMainContext.js timeout | 全局 timeout:8000 + onLoad 轻量化 |
| WXML filterTasks() 函数调用不生效 | 改为 JS 预计算 _filteredTasks/Procurements |
| 参与人数统计含已取消 | 新增 canceled 计数，减去已取消 |
| 团队编辑缺少 API | 新增 TeamUpdate schema + PUT 端点 |

### 后端改动
| 文件 | 变更 |
|------|------|
| `models/team.py` | 新增 ProcurementItem, ProcurementClaim |
| `schemas/team.py` | 新增 6 个采购 Schema + TeamUpdate + TeamActivityFullOut 扩展 |
| `routers/team.py` | 新增 12 个端点（采购 CRUD + 认领 + 统计 + auto-join + 团队编辑/撤回/恢复） |
| `routers/stats.py` | 修复成就查询 MultipleResultsFound |
| `core/__init__.py` | v1.0.5 → v1.1.1 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/activity_detail/` | 新增第 5 Tab「采购」+ 3 个弹窗 + WXSS 样式 |
| `pages/team_detail/` | 编辑/撤回/恢复团队 + 活动编辑/下架 + 转发按钮 + WXSS 样式 + dialog-mask |
| `pages/teams/` | 归档模式切换 + 群主标识修复 + dialog-mask |
| `pages/profile/` | 已撤回团队入口 + 导航修复 |
| `utils/api.js` | 新增 12 个 API 方法 + 全局 timeout:8000 |
| `app.js` | baseUrl 更新 |
| `app.wxss` | 全局 input-field 优化 |

---

## v1.0.5 (2026-06-03)

### 修复

**UI 交互优化：**
- 创建团队对话框加宽（92%/500px），输入框增高防文字截断
- 切换 Tab 时自动关闭所有弹窗（发布活动/发布公告/留言板）
- 补全 teams 页面缺失的弹窗通用样式（dialog-header/title/close）

### 部署

- 后端已部署至阿里云服务器（39.105.83.103）
- 域名 kacumoto.com.cn 配置 HTTPS（Let's Encrypt SSL）
- 小程序体验版通过合法域名验证正常通信
- 后端 systemd 开机自启 + certbot 证书自动续期

### 后端改动
| 文件 | 变更 |
|------|------|
| `core/__init__.py` | v1.0.4 → v1.0.5 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/teams/teams.wxss` | 新增 dialog-box/dialog-header/dialog-title/dialog-close 样式 |
| `pages/teams/teams.wxml` | 创建/加入团队弹窗使用 dialog-wide 类 |
| `pages/team_detail/team_detail.js` | switchTab 自动关闭所有弹窗 |
| `app.wxss` | 全局 input-field 增加 min-height 和字号 |
| `app.js` | baseUrl 改为 https://kacumoto.com.cn |

---

## v1.0.4 (2026-05-25)

### 新功能：成员留言板

**成员留言：**
- 新增 TeamMessage 模型，团队成员可互相留言
- 点击成员卡片弹出留言板对话框，查看/发送/删除留言
- 留言对全体团队成员可见（非私密）
- 权限校验：留言对象和留言者都必须是团队成员

### 后端改动
| 文件 | 变更 |
|------|------|
| `models/team.py` | 新增 TeamMessage 模型（team_messages 表） |
| `models/__init__.py` | 新增 TeamMessage 导入 |
| `schemas/team.py` | 新增 TeamMessageCreate/Out |
| `routers/team.py` | 新增 3 个留言端点（GET 列表/POST 留言/DELETE 删除） |
| `core/__init__.py` | v1.0.3 → v1.0.4 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/team_detail/team_detail.js` | 新增留言 dialog 数据 + 6 个处理方法 |
| `pages/team_detail/team_detail.wxml` | 成员卡片 tap 事件 + 留言板对话框 |
| `pages/team_detail/team_detail.wxss` | 新增留言列表/输入/删除样式 |
| `utils/api.js` | 新增 3 个留言 API 封装 |

---

## v1.0.3 (2026-05-25)

### 新功能：活动深度扩展（Phase 3）

**物资调配：**
- 新增 ActivityResource 模型，支持活动物资创建/分类/数量管理
- 新增 ResourceAssignment 模型，物资分派到成员，实时跟踪分配进度
- 分派时自动递增 assigned_quantity 冗余字段，支持总量校验
- 分派超量保护：自动拒绝超出总量的分派请求
- 分派通知自动推送至被分派人

**场地布设：**
- 新增 ActivityVenue 模型（一对一场地），含地址/容量/联系人/布设时间/要求
- 新增 VenueSetupItem 模型，场地布设子项清单（可勾选完成）
- 一个活动仅限一个场地（unique 约束），可随时创建/修改/删除

**活动子任务：**
- 新增 ActivityTask 模型，支持优先级（普通/重要/紧急）和状态流转
- 状态快捷更新端点（待处理 → 进行中 → 已完成 → 已取消）
- 按优先级降序 + 截止日升序排列，我的任务独立端点
- 任务分配自动通知至负责人

**聚合详情端点：**
- `GET /activities/{id}/full-detail` 一次性返回活动+任务+物资+分派+场地+布设子项
- 前端 activity_detail 页面（4 Tab），避免 N+1 查询

**新页面 activity_detail：**
- 4 Tab：概览（活动信息 + 参与人 + 评论）、物资、场地、任务
- 各 Tab 完整 CRUD 弹窗表单，遵循深色主题 UI
- 团队详情页活动卡片增加「详情」按钮导航

### 后端改动
| 文件 | 变更 |
|------|------|
| `models/team.py` | 新增 ActivityTask、ActivityResource、ResourceAssignment、ActivityVenue、VenueSetupItem |
| `models/__init__.py` | 新增 5 个模型导入 |
| `schemas/team.py` | 新增 6 个 Schema及 TeamActivityFullOut 聚合 |
| `routers/team.py` | 新增 ~20 个端点（task/resource/assignment/venue/setup CRUD + full-detail） |
| `core/__init__.py` | v1.0.2 → v1.0.3 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/activity_detail/` | 新页面（4 文件）：4 Tab 活动详情 |
| `pages/team_detail/` | 活动卡片增加「详情」按钮导航 |
| `utils/api.js` | 新增 20 个 API 方法 |
| `app.json` | 注册 activity_detail 页面 |

---

## v1.0.2 (2026-05-25)

### 新功能：团队协作增强（Phase 2）

**通知系统：**
- 新增通知中心页面（`pages/notifications`），支持通知列表、已读/未读状态
- 后端 Notification 模型 + 全套 CRUD API（列表、未读数、标记已读、全部已读）
- 通知自动生成：报名确认、签到确认、新评论、新公告时自动推送通知
- 首页 TabBar badge 红点提示未读数量
- 个人中心通知入口（显示未读数 + 红点）

**团队动态 Feed：**
- 新增 TeamFeedItem 模型，自动记录团队成员操作（加入、发布活动、报名、签到、公告）
- `/_create_feed_item()` 装饰器模式嵌入现有业务逻辑
- 首页全局 Feed 流展示（按 action_type 显示对应图标）
- 团队专属 Feed 端点 `GET /{team_id}/feed`

**活动评论：**
- 新增 ActivityComment 模型，活动卡片可展开评论
- 评论发布/加载/计数，实时更新
- 后端完整 CRUD（新增/列表/删除）

**团队公告：**
- 新增 TeamAnnouncement 模型，团队详情"公告"Tab
- 公告创建/列表展示，置顶标记
- 团队详情 Tab 从 2 个扩展为 3 个（活动/公告/成员）

### Bug 修复
- 修复 WXML 不兼容语法：`[0].toUpperCase()`、`?.` 可选链、`[]` 括号属性访问
- 修复 WXML 表达式 `item.name[0]` → 改用预计算 `item.initial`
- 修复 `eventTypes[item.event_type]` 动态键访问 → 改用预计算 `item.typeTag`
- 修复 inline style `animation-delay:{{index * 0.1}}s` 在 iOS 下的兼容问题
- 修复 TabBar 图标格式（SVG → PNG，微信不支持 tabBar SVG）
- 修复旧数据库缺少 Phase 2 新增字段（删库重建 schema）

### 后端改动
| 文件 | 变更 |
|------|------|
| `models/team.py` | 新增 TeamFeedItem、ActivityComment、TeamAnnouncement |
| `models/notification.py` | 新文件：Notification 模型 |
| `models/__init__.py` | 新增模型导入 |
| `schemas/team.py` | 新增 Feed/Comment/Announcement 全套 Schema |
| `schemas/notification.py` | 新文件：NotificationOut、UnreadCountOut |
| `routers/team.py` | Feed 自动生成 hooks + 20+ 新端点 |
| `routers/notification.py` | 新文件：通知 CRUD |
| `main.py` | 注册 notification 路由 |
| `core/__init__.py` | v1.0.1 → v1.0.2 |

### 前端改动
| 文件 | 变更 |
|------|------|
| `pages/notifications/` | 新页面（4 文件） |
| `pages/team_detail/` | 增强：3 Tab、公告、评论 |
| `pages/index/` | 全局 Feed 流、TabBar badge |
| `pages/profile/` | 通知入口（未读数 + 红点） |
| `pages/teams/` | 修复 `item.name[0]` 语法 |
| `pages/special_events/` | 修复 `eventTypes[]` 语法 |
| `pages/special_event_detail/` | 修复 `eventTypes[]` + `[0]` 语法 |
| `utils/api.js` | 新增 15 个 API 方法 |
| `app.js` | baseUrl 端口修正 |
| `app.json` | TabBar 图标格式修正 |
| `project.config.json` | 添加 miniprogramRoot，关闭 urlCheck |

---

## v1.0.1 (2026-05-24)

### Bug 修复
- 修复习惯打卡缺少 `record_date` 参数导致后端 422 错误
- 修复运动 CSV 导出引用不存在的 `sport_type` 字段（改为 `sport_type_id`）
- 修复专项活动页面变量名拼写错误 (`itemTypes` → `eventTypes`)
- 修复任务列表 `is_completed` 过滤逻辑（改用 `status === 2`）
- 修复任务详情显示 `note` 字段引用（后端返回 `description`）
- 修复运营活动详情页字段名错误 (`desc`/`rules` → `summary`/`detail`)
- 修复运营活动报名数显示（改用 `checkin-stats` API 获取真实数据）
- 修复创建任务时参数 `note` → `description` 映射

### 重构：移除冗余模块（Phase 1）
根据项目重新定位（团队活动跟踪 → 移除个人健身/任务习惯/运营活动模块）：

**后端删除：**
- 删除 `models/workout.py`、`models/habit.py`、`models/ops.py`
- 删除 `routers/workout.py`、`routers/habit.py`、`routers/ops.py`
- 删除 `schemas/workout.py`、`schemas/habit.py`、`schemas/ops.py`
- 删除 `_debug_event.py`

**后端修改：**
- `models/__init__.py`：移除已删除模型的导入
- `models/user.py`：简化 UserSettings（移除健身相关字段，保留通知设置）
- `schemas/user.py`：UserSettings 同步简化
- `main.py`：路由从 7 个缩减为 4 个（user/team/event/stats）
- `routers/stats.py`：重写为纯团队向成就系统（7 种成就）

**前端删除：**
- 删除 8 个页面目录：workout、workout_log、tasks、habits、ops_list、ops_detail、ops_enroll、ops_ticket
- 删除 4 个 Tab 图标：tab_workout、tab_workout_active、tab_tasks、tab_tasks_active

**前端修改：**
- `app.json`：页面从 15 个缩减为 7 个，Tab 从 5 个缩减为 4 个
- `utils/api.js`：API 从 40+ 缩减为 ~35 个（仅保留 user/team/event/achievement）
- `pages/index/index.js`：移除运动/任务逻辑，改为加载团队列表和最近活动
- `pages/index/index.wxml`：移除打卡卡片/周趋势/任务列表，替换为团队列表和最近活动
- `pages/index/index.wxss`：移除旧样式，添加团队/活动列表样式
- `pages/profile/profile.js`：移除运动统计/导出/每日目标方法
- `pages/profile/profile.wxml`：移除运动总览/运营活动入口/导出入口

### 变更文件
| 文件 | 变更类型 |
|------|:--------:|
| `backend/app/core/__init__.py` | 版本号更新 |
| `backend/app/routers/stats.py` | Bug 修复 |
| `miniprogram/utils/api.js` | Bug 修复 + 新增 API |
| `miniprogram/pages/habits/habits.js` | Bug 修复 |
| `miniprogram/pages/tasks/tasks.js` | Bug 修复 |
| `miniprogram/pages/tasks/tasks.wxml` | Bug 修复 |
| `miniprogram/pages/special_events/special_events.wxml` | Bug 修复 |
| `miniprogram/pages/ops_detail/ops_detail.js` | Bug 修复 |
| `miniprogram/pages/ops_detail/ops_detail.wxml` | Bug 修复 |

---

## v1.0.0 (初始版本)

项目初始化完成，实现全部核心功能模块。

### 后端
- 用户体系（登录/注册/设置）
- 健身运动（记录/打卡/统计/连续天数/CSV导出）
- 团队活动（创建/加入/活动发布/报名/签到）
- 任务习惯（CRUD/打卡/复盘）
- 专项挑战（创建/进度/排行榜）
- 成就系统（13种成就自动颁发）
- 活动运营（完整审批流/报名/签到/礼品/赞助商）

### 前端
- 5 Tab 导航 + 15 页面
- 首页仪表板（问候/打卡卡片/周趋势/待办预览）
- 深色主题 UI + 动画 + 骨架屏
- 完整 API 封装（40+ 接口）
